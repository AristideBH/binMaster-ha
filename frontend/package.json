{
  "name": "binmaster-card",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "watch": "node build.mjs --watch"
  },
  "dependencies": {
    "lit": "^3.1.0",
    "custom-card-helpers": "^2.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
